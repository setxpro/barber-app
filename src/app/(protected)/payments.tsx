import { View, Text } from "react-native";

export default function Payments() {
    return (
        <View style={{flex: 1, justifyContent: 'center', alignItems: 'center'}}>
            <Text style={{fontFamily: 'light'}}>Pagamentos</Text>
        </View>
    )
}